<%= form_with(model: game, local: true) do |form| %>
  <% if game.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(game.errors.count, "error") %> prohibited this game from being saved:</h2>

      <ul>
        <% game.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :game_state %>
    <%= form.text_field :game_state %>
  </div>

  <div class="field">
    <%= form.label :word %>
    <%= form.text_field :word %>
  </div>

  <div class="field">
    <%= form.label :closed_at %>
    <%= form.datetime_select :closed_at %>
  </div>

  <div class="field">
    <%= form.label :black %>
    <%= form.text_field :black %>
  </div>

  <div class="field">
    <%= form.label :white %>
    <%= form.text_field :white %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
